{
  "name": "api-tests",
  "version": "1.0.0",
  "description": "API tests",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "format": "prettier --single-quote --write test-suites/*"
  },
  "dependencies": {
    "dotenv": "^5.0.1",
    "jest": "^22.4.2",
    "lodash": "^4.17.5",
    "superagent": "^3.8.2"
  },
  "devDependencies": {
    "eslint": "^4.18.1",
    "prettier": "^1.11.0"
  },
  "eslintConfig": {
    "root": true,
    "extends": "eslint:recommended",
    "env": {
      "es6": true,
      "node": true,
      "jest": true
    },
    "rules": {
      "no-shadow": "warn",
      "no-console": "off"
    }
  }
}
